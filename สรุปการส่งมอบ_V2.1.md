# สรุปการส่งมอบระบบคำนวณคอมมิชชั่น ATMO'decor v2.1

## 📅 ข้อมูลการส่งมอบ

- **เวอร์ชัน**: 2.1
- **วันที่**: 11 มกราคม 2026
- **GitHub Repository**: https://github.com/pz1491/atmo-commission-system

---

## ✅ การอัพเดทในเวอร์ชัน 2.1

### 1. แก้ไขการคำนวณคอมมิชชั่นหลัก
- **เปลี่ยนจาก**: คำนวณจากยอดรวมทั้งหมด
- **เป็น**: **คำนวณจากส่วนต่างที่เกิน 20,000 บาท**
- **ตัวอย่าง**: 
  - ยอด 20,537 บาท → ส่วนต่าง 537 บาท
  - คอมมิชชั่น 1% = 537 × 1% = 5.37 บาท

### 2. ปรับยอดขั้นต่ำ
- **เปลี่ยนจาก**: 25,000 บาท
- **เป็น**: **20,000 บาท**

### 3. ปรับการนับรายการแจกัน (Add on 2vases)
- **ตรวจจากข้อความออเดอร์** แทนการระบุแยก
- นับจำนวนบรรทัดที่มีคำว่า "แจกัน" หรือ "vase"
- ไม่นับ "Mini vase" หรือแจกันราคาต่ำกว่า 4,500 บาท
- ถ้ามี ≥ 2 รายการ → Add on +500 บาท (หรือ +300 ถ้ายอด >9,500)

### 4. เพิ่ม Docker Support
- เพิ่ม `Dockerfile` สำหรับ containerization
- เพิ่ม `docker-compose.yml` สำหรับ deployment ง่าย
- รองรับการรันบนเซิร์ฟเวอร์

### 5. เพิ่มเอกสาร Deploy
- สร้าง `DEPLOY.md` พร้อมคำแนะนำทีละขั้นตอน
- รวมการตั้งค่า SSL, Nginx, Firewall
- มีส่วน Troubleshooting และ Monitoring

### 6. Push ขึ้น GitHub
- สร้าง public repository: `atmo-commission-system`
- พร้อม README, CHANGELOG, และเอกสารครบถ้วน

---

## 📊 เงื่อนไขการคำนวณปัจจุบัน

### คอมมิชชั่นหลัก (1-4%)
- **ยอดขั้นต่ำ**: 20,000 บาท
- **คำนวณจาก**: ส่วนต่างที่เกิน 20,000 บาท
- **เรท**:
  - 20,000-49,999 → 1%
  - 50,000-69,999 → 2%
  - 70,000-99,999 → 2%
  - 100,000-179,999 → 3%
  - 180,000+ → 4%

### Add on (order) - อัตโนมัติ
- 3 ออเดอร์ → +100 บาท
- 6 ออเดอร์ → +400 บาท
- 8 ออเดอร์ → +800 บาท
- 12 ออเดอร์ → +1,500 บาท

### Add on (2vases) - ตรวจจากข้อความ
- ถ้ามีแจกัน ≥ 2 รายการ (ราคา ≥ 4,500 บาท, ไม่รวม Mini vase)
- ยอด ≤ 9,500 → +500 บาท
- ยอด > 9,500 → +300 บาท

### คอมมิชชั่นพิเศษ (5%)
- แจกันฟาแลน (ยกเว้น Ikebana Curve)
- Ikebana Curve ทุกรุ่น
- ชุดดอกไม้อย่างเดียว (≥ 8,000 บาท)

### OT Penalty
- ช่วง 18:00-22:00 ต้องมียอด ≥ 5,000 บาท
- ถ้าไม่ถึง → หัก 30% (สูงสุด 300 บาท)

### Incentive ต่อคน
- **คอมมิชชั่นรวม ÷ จำนวนคนตอบ**

---

## 📦 ไฟล์ที่ส่งมอบ

### โครงสร้างโปรเจกต์
```
line-commission-system/
├── app.py                          # แอปพลิเคชันหลัก
├── Dockerfile                      # Docker configuration
├── docker-compose.yml              # Docker Compose configuration
├── requirements.txt                # Python dependencies
├── .env.example                    # ตัวอย่างการตั้งค่า
├── .gitignore                      # Git ignore rules
│
├── src/
│   ├── __init__.py
│   ├── commission_calculator.py   # โมดูลคำนวณคอมมิชชั่น (v2.1)
│   ├── database.py                 # โมดูลจัดการฐานข้อมูล
│   └── line_handler.py             # โมดูล LINE webhook handler
│
├── tests/
│   ├── test_commission.py          # สคริปต์ทดสอบ v1
│   └── test_commission_v2.py       # สคริปต์ทดสอบ v2
│
├── data/                           # โฟลเดอร์เก็บข้อมูล (สร้างอัตโนมัติ)
├── images/                         # โฟลเดอร์เก็บรูปภาพ (สร้างอัตโนมัติ)
│
└── เอกสาร/
    ├── README.md                   # คู่มือการใช้งานหลัก
    ├── README_V2.md                # คู่มือเวอร์ชัน 2.0
    ├── QUICKSTART.md               # เริ่มต้นใช้งานใน 5 นาที
    ├── DEPLOY.md                   # คู่มือการ Deploy บนเซิร์ฟเวอร์
    ├── CHANGELOG_V2.md             # บันทึกการเปลี่ยนแปลง
    ├── DELIVERY.md                 # เอกสารส่งมอบ (ภาษาอังกฤษ)
    ├── สรุปการส่งมอบ_V2.md         # สรุปการส่งมอบ v2.0
    ├── สรุปการส่งมอบ_V2.1.md       # สรุปการส่งมอบ v2.1 (ไฟล์นี้)
    ├── design.md                   # เอกสารออกแบบ v1
    └── design-v2.md                # เอกสารออกแบบ v2
```

---

## 🚀 การเริ่มใช้งาน

### วิธีที่ 1: รันบนเครื่องตัวเอง (Development)

```bash
# 1. Clone repository
git clone https://github.com/pz1491/atmo-commission-system.git
cd atmo-commission-system

# 2. ติดตั้ง dependencies
pip3 install -r requirements.txt

# 3. ตั้งค่า .env
cp .env.example .env
nano .env  # แก้ไข LINE_CHANNEL_ACCESS_TOKEN และ LINE_CHANNEL_SECRET

# 4. รันระบบ
python3 app.py
```

### วิธีที่ 2: Deploy บนเซิร์ฟเวอร์ด้วย Docker (Production)

```bash
# 1. Clone repository
git clone https://github.com/pz1491/atmo-commission-system.git
cd atmo-commission-system

# 2. ตั้งค่า .env
cp .env.example .env
nano .env

# 3. รัน Docker Compose
docker-compose up -d

# 4. ตรวจสอบสถานะ
docker-compose logs -f
```

**อ่านรายละเอียดเพิ่มเติมใน `DEPLOY.md`**

---

## 🧪 การทดสอบ

### ทดสอบการคำนวณ

```bash
# รันสคริปต์ทดสอบ
python3 tests/test_commission_v2.py
```

### ทดสอบกับ LINE

1. เพิ่ม Bot เข้ากลุ่ม LINE
2. ส่งคำสั่ง `/start` เพื่อเริ่มต้นวัน
3. ส่งข้อความออเดอร์ตามรูปแบบ:
   ```
   1.Visa Patanasin/fb
   
   แจกันลายมนกุหลาบขาว 4 ดอก 
   4580 11/1 kbank 15:27
   คุณ วิสาข์ พัฒนสิน
   ...
   ```
4. ระบบจะตอบกลับพร้อมสรุปคอมมิชชั่น

---

## 📝 ตัวอย่างการคำนวณ

### ตัวอย่างที่ 1: ยอด 20,537 บาท (ตามไฟล์ Excel)

**ข้อมูล:**
- ยอดรวม: 20,537 บาท
- จำนวนออเดอร์: 3 ออเดอร์
- จำนวนคน: 4 คน
- แจกัน 2 รายการ: 2 ออเดอร์

**การคำนวณ:**
1. คอมมิชชั่น 1%: (20,537 - 20,000) × 1% = **5.37 บาท**
2. Add on (order): 3 ออเดอร์ → **100 บาท**
3. Add on (2vases): 2 ออเดอร์ × 100 = **200 บาท**
4. **รวม**: 5.37 + 100 + 200 = **305.37 บาท**
5. **Incentive ต่อคน**: 305.37 ÷ 4 = **76.34 บาท**

---

## 🔗 ลิงก์สำคัญ

- **GitHub Repository**: https://github.com/pz1491/atmo-commission-system
- **LINE Developers Console**: https://developers.line.biz/console/
- **Docker Hub**: https://hub.docker.com/

---

## 📞 การติดต่อและสนับสนุน

หากพบปัญหาหรือต้องการความช่วยเหลือ:

1. **ตรวจสอบเอกสาร**:
   - `README_V2.md` - คู่มือการใช้งานฉบับเต็ม
   - `DEPLOY.md` - คู่มือการ Deploy
   - `CHANGELOG_V2.md` - บันทึกการเปลี่ยนแปลง

2. **ตรวจสอบ Logs**:
   ```bash
   # ถ้ารันด้วย Python
   tail -f app.log
   
   # ถ้ารันด้วย Docker
   docker-compose logs -f
   ```

3. **ทดสอบระบบ**:
   ```bash
   python3 tests/test_commission_v2.py
   ```

4. **GitHub Issues**: สร้าง issue ใน repository

---

## ✅ Checklist การใช้งาน

### สำหรับ Development
- [ ] Clone repository จาก GitHub
- [ ] ติดตั้ง Python dependencies
- [ ] ตั้งค่าไฟล์ `.env`
- [ ] รันสคริปต์ทดสอบ
- [ ] รัน `python3 app.py`
- [ ] ตั้งค่า Webhook URL ใน LINE Developers
- [ ] ทดสอบส่งข้อความใน LINE

### สำหรับ Production
- [ ] เตรียมเซิร์ฟเวอร์ (VPS/Cloud)
- [ ] ติดตั้ง Docker และ Docker Compose
- [ ] Clone repository
- [ ] ตั้งค่าไฟล์ `.env`
- [ ] รัน `docker-compose up -d`
- [ ] ตั้งค่า SSL (Nginx + Let's Encrypt)
- [ ] ตั้งค่า Webhook URL (HTTPS)
- [ ] ตั้งค่า Firewall
- [ ] ทดสอบส่งข้อความใน LINE
- [ ] ตั้งค่า Backup อัตโนมัติ
- [ ] Monitor logs และ resource usage

---

## 🎉 สรุป

ระบบคำนวณคอมมิชชั่น ATMO'decor v2.1 พร้อมใช้งานแล้ว! 

**คุณสมบัติหลัก:**
- ✅ คำนวณคอมมิชชั่นจากส่วนต่างที่เกิน 20,000 บาท
- ✅ ตรวจจับรายการแจกันอัตโนมัติ
- ✅ รองรับ Docker deployment
- ✅ มีเอกสารครบถ้วน
- ✅ Push ขึ้น GitHub แล้ว
- ✅ พร้อม Deploy บนเซิร์ฟเวอร์

**ขอบคุณที่ใช้บริการ!** 🌸
