# 🌸 ระบบคำนวณคอมมิชชั่น ATMO'decor

ระบบคำนวณคอมมิชชั่นอัตโนมัติสำหรับ ATMO'decor ที่เชื่อมต่อกับ LINE Official Account เพื่อรับข้อมูลออเดอร์จากกลุ่ม คำนวณคอมมิชชั่นตามเงื่อนไขที่กำหนด และตอบกลับสรุปคอมมิชชั่นทันที

## ✨ คุณสมบัติ

- ✅ **รับออเดอร์อัตโนมัติ**: รับข้อความออเดอร์จากกลุ่ม LINE OA
- ✅ **คำนวณคอมมิชชั่น**: คำนวณตามเงื่อนไขที่ซับซ้อน (เรทหลัก + โบนัสออเดอร์ + คอมมิชชั่นพิเศษ)
- ✅ **สะสมยอดรายวัน**: เก็บยอดขายและจำนวนออเดอร์ตลอดวัน
- ✅ **รีเซ็ตอัตโนมัติ**: รีเซ็ตข้อมูลเป็น 0 ทุกวันเวลา 23:59 น.
- ✅ **ตอบกลับทันที**: แสดงสรุปคอมมิชชั่นหลังบันทึกออเดอร์
- ✅ **รองรับคอมมิชชั่นพิเศษ**: Ikebana Curve, ชุดดอกไม้อย่างเดียว, แจกัน 2 ใบขึ้นไป

## 📋 กฎการคำนวณคอมมิชชั่น

### คอมมิชชั่นหลัก (ตามยอดขายสะสม)

| ยอดขายสะสม | เรทคอมมิชชั่น | โบนัสออเดอร์ |
|------------|---------------|--------------|
| 20,000 - 49,999 | 1% | 3 ออเดอร์+ → +100 บาท |
| 50,000 - 99,999 | 2% | 6 ออเดอร์+ → +400 บาท |
| 100,000 - 179,999 | 3% | 8 ออเดอร์+ → +800 บาท |
| 180,000+ | 4% | 12 ออเดอร์+ → +1,500 บาท |

**หมายเหตุ**: ยอดขายต้อง > 20,000 บาทจึงจะได้รับคอมมิชชั่น

### คอมมิชชั่นพิเศษ

1. **ลูกค้าซื้อ 2 แจกันขึ้นไป**: +500 บาท
   - เฉพาะแจกันราคา ≥ 4,500 บาท/ใบ
   - ไม่รวม Mini vase และน้ำหอม

2. **ชุดดอกไม้อย่างเดียว** (ไม่รวมแจกัน): 5%
   - ราคา ≥ 8,000 บาท
   - ถ้าราคา < 8,000 → ใช้เรทปกติ

3. **แจกัน Ikebana Curve**: 5%
   - ทุกสี ทุกรุ่น

4. **น้ำหอมอย่างเดียว**: ไม่นับเป็นออเดอร์

## 🚀 การติดตั้ง

### ขั้นตอนที่ 1: ติดตั้ง Dependencies

```bash
# ติดตั้ง Python packages
pip3 install -r requirements.txt
```

### ขั้นตอนที่ 2: ตั้งค่า LINE Official Account

1. ไปที่ [LINE Developers Console](https://developers.line.biz/console/)
2. สร้าง Provider (ถ้ายังไม่มี)
3. สร้าง Channel ประเภท "Messaging API"
4. ในหน้า Channel Settings:
   - คัดลอก **Channel Secret**
   - ไปที่แท็บ "Messaging API"
   - คัดลอก **Channel Access Token** (ถ้ายังไม่มีให้กด Issue)

### ขั้นตอนที่ 3: ตั้งค่า Environment Variables

แก้ไขไฟล์ `.env`:

```bash
# LINE Official Account Configuration
LINE_CHANNEL_ACCESS_TOKEN=YOUR_CHANNEL_ACCESS_TOKEN_HERE
LINE_CHANNEL_SECRET=YOUR_CHANNEL_SECRET_HERE

# Server Configuration
PORT=5000
FLASK_ENV=development

# Timezone
TIMEZONE=Asia/Bangkok
```

### ขั้นตอนที่ 4: รันระบบ

```bash
python3 app.py
```

ระบบจะรันที่ `http://localhost:5000`

### ขั้นตอนที่ 5: ตั้งค่า Webhook URL

#### วิธีที่ 1: ใช้ ngrok สำหรับทดสอบ (Local)

```bash
# ติดตั้ง ngrok
# ไปที่ https://ngrok.com/ และดาวน์โหลด

# รัน ngrok
ngrok http 5000
```

คัดลอก URL ที่ได้ (เช่น `https://xxxx-xx-xx-xx-xx.ngrok-free.app`)

#### วิธีที่ 2: Deploy บน Cloud (Production)

**ตัวอย่าง: Deploy บน Railway**

1. ไปที่ [Railway.app](https://railway.app/)
2. สร้างโปรเจกต์ใหม่จาก GitHub repo
3. เพิ่ม Environment Variables (LINE_CHANNEL_ACCESS_TOKEN, LINE_CHANNEL_SECRET)
4. Deploy จะได้ URL เช่น `https://your-app.railway.app`

**ตัวอย่างอื่นๆ**: Heroku, Render, Google Cloud Run, AWS

#### ตั้งค่า Webhook ใน LINE Developers Console

1. ไปที่ Channel Settings → Messaging API
2. ใน "Webhook settings":
   - Webhook URL: `https://your-domain.com/webhook`
   - เปิด "Use webhook": ON
   - กด "Verify" เพื่อทดสอบ
3. ใน "LINE Official Account features":
   - ปิด "Auto-reply messages": OFF
   - ปิด "Greeting messages": OFF (ถ้าไม่ต้องการ)

### ขั้นตอนที่ 6: เพิ่ม Bot เข้ากลุ่ม

1. สแกน QR Code ของ LINE OA เพื่อเพิ่มเป็นเพื่อน
2. เพิ่ม Bot เข้ากลุ่มที่ต้องการใช้งาน
3. ทดสอบส่งข้อความออเดอร์

## 📱 วิธีใช้งาน

### ส่งออเดอร์

เพียงส่งข้อความออเดอร์ตามปกติในกลุ่ม LINE ระบบจะแยกวิเคราะห์และคำนวณคอมมิชชั่นอัตโนมัติ

**ตัวอย่างข้อความ:**

```
1. Mini vaseทิวลิปส้ม 1
3,000 13:40 bay 10/01
คุณ สลิลเกตน์ โลกะวิทย์
2882/660 คอนโดศุภาลัยปาร์ค เอกมัย-ทองหล่อ
ถนนเพชรบุรีตัดใหม่ แขวงบางกะปิ เขตห้วยขวาง
กทม. 10310
โทร. 0898113968
```

ระบบจะตอบกลับด้วยสรุปคอมมิชชั่น:

```
✅ บันทึกออเดอร์สำเร็จ!

📦 ออเดอร์นี้:
• สินค้า: 1. Mini vaseทิวลิปส้ม 1
• ยอดขาย: 3,000 บาท
• คอมมิชชั่น: 0 บาท (ยอดยังไม่ถึง 20,000)

📊 สรุปวันนี้:
• ยอดขายสะสม: 15,000 บาท
• จำนวนออเดอร์: 3 ออเดอร์
• เรทปัจจุบัน: 0%
• เป้าหมายถัดไป: 20,000 บาท (1%)
• คอมมิชชั่นสะสม: 0 บาท
```

### คำสั่งพิเศษ

| คำสั่ง | คำอธิบาย |
|--------|----------|
| `/summary` หรือ `/สรุป` | แสดงสรุปยอดขายและคอมมิชชั่นวันนี้ |
| `/reset` หรือ `/รีเซ็ต` | รีเซ็ตข้อมูลเป็น 0 (ใช้ด้วยความระมัดระวัง) |
| `/help` หรือ `/ช่วยเหลือ` | แสดงคำสั่งที่ใช้ได้ |

## 🧪 การทดสอบ

รันสคริปต์ทดสอบ:

```bash
python3 tests/test_commission.py
```

ผลลัพธ์ที่คาดหวัง:

```
==================================================
ทดสอบการดึงยอดเงิน
==================================================
✅ '3,000 บาท' -> 3000.0 (คาดหวัง: 3000)
✅ '15000' -> 15000.0 (คาดหวัง: 15000)
...
==================================================
✅ ทดสอบเสร็จสิ้น
==================================================
```

## 📂 โครงสร้างโปรเจกต์

```
line-commission-system/
├── app.py                      # แอปพลิเคชันหลัก
├── requirements.txt            # Python dependencies
├── .env                        # Environment variables (ไม่ commit)
├── .env.example               # ตัวอย่าง environment variables
├── .gitignore                 # Git ignore file
├── README.md                  # คู่มือนี้
├── design.md                  # เอกสารออกแบบระบบ
├── src/
│   ├── __init__.py
│   ├── commission_calculator.py  # โมดูลคำนวณคอมมิชชั่น
│   ├── database.py              # โมดูลจัดการฐานข้อมูล
│   └── line_handler.py          # โมดูล LINE webhook handler
├── tests/
│   └── test_commission.py      # สคริปต์ทดสอบ
└── data/
    ├── sales_data.json         # ข้อมูลยอดขายปัจจุบัน
    └── archive_*.json          # ข้อมูลย้อนหลัง (สำรอง)
```

## 🔧 การแก้ไขปัญหา

### ปัญหา: Webhook ไม่ทำงาน

1. ตรวจสอบว่า Webhook URL ถูกต้อง
2. ตรวจสอบว่าเซิร์ฟเวอร์รันอยู่
3. ตรวจสอบ logs ใน console
4. ใช้ "Verify" ใน LINE Developers Console

### ปัญหา: Bot ไม่ตอบกลับ

1. ตรวจสอบว่าปิด "Auto-reply messages" แล้ว
2. ตรวจสอบว่า Channel Access Token ถูกต้อง
3. ตรวจสอบว่าข้อความมียอดเงิน (ตัวเลข)

### ปัญหา: คำนวณคอมมิชชั่นไม่ถูกต้อง

1. รันสคริปต์ทดสอบ: `python3 tests/test_commission.py`
2. ตรวจสอบ logs ใน console
3. ตรวจสอบข้อมูลใน `data/sales_data.json`

## 📊 การดูข้อมูล

### ดูข้อมูลปัจจุบัน

```bash
cat data/sales_data.json
```

### ดูข้อมูลย้อนหลัง

```bash
ls data/archive_*.json
cat data/archive_2026-01-10.json
```

### Health Check

เปิดเบราว์เซอร์ไปที่: `http://localhost:5000/health`

```json
{
  "status": "ok",
  "date": "2026-01-11",
  "total_sales": 45000,
  "total_orders": 5
}
```

## 🔐 ความปลอดภัย

- ✅ เก็บ Channel Secret และ Access Token ใน `.env` (ไม่ commit ลง Git)
- ✅ ตรวจสอบ signature ของ webhook
- ✅ ใช้ HTTPS สำหรับ webhook URL
- ✅ จำกัด IP ที่เข้าถึง webhook (ถ้าเป็น production)

## 📝 หมายเหตุ

- ระบบจะรีเซ็ตข้อมูลอัตโนมัติทุกวันเวลา 23:59 น.
- ข้อมูลเก่าจะถูกบันทึกไว้ใน `data/archive_*.json`
- ถ้าระบบปิดข้ามวัน จะรีเซ็ตอัตโนมัติเมื่อเริ่มต้นใหม่
- สามารถใช้คำสั่ง `/reset` เพื่อรีเซ็ตด้วยตนเอง (ใช้ด้วยความระมัดระวัง)

## 🆘 การขอความช่วยเหลือ

หากมีปัญหาหรือข้อสงสัย:

1. ตรวจสอบ logs ใน console
2. รันสคริปต์ทดสอบ
3. ตรวจสอบเอกสารนี้อีกครั้ง
4. ติดต่อผู้พัฒนาระบบ

## 📄 License

Copyright © 2026 ATMO'decor. All rights reserved.

---

**พัฒนาโดย**: Manus AI Assistant  
**วันที่**: มกราคม 2026  
**เวอร์ชัน**: 1.0.0
