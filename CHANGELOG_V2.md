# บันทึกการเปลี่ยนแปลง - Version 2.0

## 🎉 Version 2.0 (11 มกราคม 2026)

### ✨ ฟีเจอร์ใหม่

#### 1. ระบบเริ่มต้นวัน
- **เลือกวันที่จากปฏิทิน** - ใช้ Date Picker แทนการใช้วันที่อัตโนมัติ
- **ระบุจำนวนคนตอบ** - เลือกจำนวนคน 1-5 คน
- **ระบุชื่อผู้ตอบ** - บันทึกชื่อคนที่ทำงาน (เช่น Oil, Fang, Phung)
- **ฟลาวการใช้งาน**:
  1. ส่ง `/start`
  2. เลือกวันที่จากปฏิทิน
  3. เลือกจำนวนคน
  4. ระบุชื่อ (คั่นด้วยคอมม่า)

#### 2. OT Penalty System
- **ช่วง 18:00-22:00**: ต้องมียอด ≥ 5,000 บาท
- **ถ้าไม่ถึง**: หักคอมมิชชั่น 30% (สูงสุด 300 บาท)
- **ตัวอย่าง**:
  - คอมมิชชั่น 1,000 บาท, ยอด 18:00-22:00 = 3,000 → หัก 300 บาท
  - คอมมิชชั่น 1,500 บาท, ยอด 18:00-22:00 = 2,000 → หัก 300 บาท (สูงสุด)

#### 3. การแบ่งคอมมิชชั่นอัตโนมัติ
- **สูตร**: คอมมิชชั่นรวม ÷ จำนวนคน = Incentive ต่อคน
- **แสดงผล**: แสดง Incentive ต่อคนในทุกข้อความยืนยัน
- **ตัวอย่าง**: คอมมิชชั่น 1,000 บาท ÷ 2 คน = 500 บาท/คน

#### 4. ระบบจัดการรูปภาพ
- **อัพโหลดรูป**: ส่งรูปพร้อมข้อความออเดอร์
- **เก็บรูป**: บันทึกในโฟลเดอร์ `data/images/`
- **ดูย้อนหลัง**: ส่ง `/images` เพื่อดูรูปทั้งหมด
- **ตั้งชื่อไฟล์**: `order_{order_id}_{timestamp}.jpg`

#### 5. คำสั่งใหม่
- `/start` - เริ่มต้นวันใหม่
- `/images` - ดูรูปภาพออเดอร์ทั้งหมด

---

### 🔄 การเปลี่ยนแปลงเงื่อนไขคอมมิชชั่น

#### คอมมิชชั่นหลัก
| เงื่อนไข | v1.0 | v2.0 |
|----------|------|------|
| ยอดขั้นต่ำ | 20,000 บาท | **25,000 บาท** |
| 25,000-49,999 | 1% + โบนัส 3 ออเดอร์ | **เหมือนเดิม** |
| 50,000-69,999 | 2% + โบนัส 6 ออเดอร์ | **เหมือนเดิม** |
| 70,000-99,999 | - | **2% + โบนัส 8 ออเดอร์ (+800)** |
| 100,000-179,999 | 3% | **เหมือนเดิม** |
| 180,000+ | 4% + โบนัส 12 ออเดอร์ | **เหมือนเดิม** |

#### Add on (2vases)
| เงื่อนไข | v1.0 | v2.0 |
|----------|------|------|
| แจกัน 2 ใบ+ (≥4,500 บาท/ใบ) | +500 บาท | **แบ่งเป็น 2 เกณฑ์** |
| ยอด ≤ 9,500 บาท | - | **+500 บาท** |
| ยอด > 9,500 บาท | - | **+300 บาท** |

#### OT Incentive (ใหม่!)
| ช่วงเวลา | เงื่อนไข | v2.0 |
|----------|----------|------|
| 18:00-22:00 | ยอด < 5,000 บาท | **หัก 30% (สูงสุด 300)** |
| 22:00-00:00 | ทุก 6,000 บาท | **+50 บาท/คน (สูงสุด 300)** |

---

### 🛠️ การเปลี่ยนแปลงทางเทคนิค

#### โครงสร้างโค้ด
1. **commission_calculator.py**
   - เพิ่มฟังก์ชัน `calculate_ot_penalty()`
   - เพิ่มฟังก์ชัน `calculate_incentive_per_person()`
   - แก้ไขเงื่อนไขคำนวณคอมมิชชั่น
   - เพิ่มค่าคงที่ `OT_EVENING_MIN_SALES = 5000`

2. **database.py**
   - เปลี่ยนเป็น `SalesDatabase` class
   - เพิ่มฟังก์ชัน `start_day()` - เริ่มต้นวัน
   - เพิ่มฟังก์ชัน `is_day_started()` - ตรวจสอบว่าเริ่มวันแล้วหรือยัง
   - เพิ่มฟังก์ชัน `save_image()` - บันทึกรูปภาพ
   - เพิ่มฟังก์ชัน `get_order_images()` - ดึงรายการรูปภาพ
   - เพิ่มฟิลด์ `staff_count`, `staff_names`, `sales_18_22`, `ot_penalty`, `incentive_per_person`

3. **line_handler.py**
   - เพิ่มฟังก์ชัน `send_start_date_picker()` - ส่ง Date Picker
   - เพิ่มฟังก์ชัน `send_staff_count_question()` - ถามจำนวนคน
   - เพิ่มฟังก์ชัน `send_staff_names_question()` - ถามชื่อ
   - เพิ่มฟังก์ชัน `send_order_confirmation()` - ยืนยันออเดอร์
   - เพิ่มฟังก์ชัน `send_images_gallery()` - แสดงรูปภาพ
   - เพิ่มฟังก์ชัน `download_image()` - ดาวน์โหลดรูปจาก LINE
   - เพิ่มระบบจัดการสถานะผู้ใช้ (user states)

4. **app.py**
   - เพิ่ม handler สำหรับ `PostbackEvent` (Date Picker)
   - เพิ่ม handler สำหรับ `ImageMessage`
   - เพิ่มฟังก์ชัน `handle_command()` - จัดการคำสั่ง
   - เพิ่มฟังก์ชัน `process_order_text()` - ประมวลผลออเดอร์
   - เพิ่มการตรวจสอบว่าเริ่มวันแล้วหรือยัง

---

### 📊 ตัวอย่างการคำนวณ

#### ตัวอย่างที่ 1: วันปกติ
```
วันที่: 11/01/2026
จำนวนคน: 2 คน (Oil, Fang)

ออเดอร์:
1. แจกัน 25,000 บาท (13:40)
2. แจกัน 30,000 บาท (15:20)

ยอดรวม: 55,000 บาท
เรท: 2%
คอมมิชชั่น 2%: 1,100 บาท
Add on (order): 0 บาท (2 ออเดอร์)

OT:
- ช่วง 18:00-22:00: 0 บาท ❌
- Penalty: -300 บาท (1,100 * 30% = 330, แต่สูงสุด 300)

รวม: 1,100 - 300 = 800 บาท
Incentive ต่อคน: 800 ÷ 2 = 400 บาท
```

#### ตัวอย่างที่ 2: วันที่มี OT
```
วันที่: 11/01/2026
จำนวนคน: 3 คน (Oil, Fang, Phung)

ออเดอร์:
1. แจกัน 25,000 บาท (13:40)
2. แจกัน 30,000 บาท (19:30) ← ช่วง OT
3. แจกัน 20,000 บาท (20:15) ← ช่วง OT

ยอดรวม: 75,000 บาท
เรท: 2%
คอมมิชชั่น 2%: 1,500 บาท
Add on (order): 100 บาท (3 ออเดอร์)

OT:
- ช่วง 18:00-22:00: 50,000 บาท ✅ (ไม่หัก)
- Penalty: 0 บาท

รวม: 1,500 + 100 = 1,600 บาท
Incentive ต่อคน: 1,600 ÷ 3 = 533.33 บาท
```

---

### 🐛 Bug Fixes

1. **แก้ไขการนับออเดอร์** - น้ำหอมไม่นับเป็นออเดอร์
2. **แก้ไขการดึงเวลา** - รองรับรูปแบบเวลาหลากหลาย (13:40, 1:40 PM, etc.)
3. **แก้ไขการคำนวณ Add on (2vases)** - แยกเกณฑ์ตามยอดเงิน

---

### ⚠️ Breaking Changes

1. **ต้องเริ่มต้นวันก่อนใช้งาน** - ส่ง `/start` ทุกครั้ง
2. **โครงสร้างข้อมูลเปลี่ยน** - ไฟล์ `sales_data.json` มีฟิลด์ใหม่
3. **ยอดขั้นต่ำเพิ่มขึ้น** - จาก 20,000 → 25,000 บาท

---

### 📦 Dependencies ใหม่

ไม่มีการเพิ่ม dependencies ใหม่ ใช้ dependencies เดิม:
- Flask
- line-bot-sdk
- python-dotenv
- APScheduler
- pytz

---

### 🔜 แผนอนาคต (v2.1)

- [ ] รองรับหลายกลุ่ม LINE
- [ ] ส่งรายงานสรุปรายวันอัตโนมัติ
- [ ] Export ข้อมูลเป็น Excel
- [ ] Dashboard สำหรับดูสถิติ
- [ ] รองรับ Flex Message สำหรับแสดงผลที่สวยงาม

---

### 📝 หมายเหตุ

- ไฟล์เวอร์ชัน 1.0 ถูกสำรองไว้ที่ `*_backup.py`
- สามารถย้อนกลับไปใช้ v1.0 ได้โดยคัดลอกไฟล์สำรองกลับมา
- ข้อมูลเก่าอาจไม่สามารถใช้งานกับ v2.0 ได้ (ต้องเริ่มต้นใหม่)

---

## 📞 ติดต่อ

หากพบปัญหาหรือมีข้อเสนอแนะ กรุณาติดต่อผู้พัฒนา

---

**ขอบคุณที่ใช้งานระบบคำนวณคอมมิชชั่น ATMO'decor!** 🌸
