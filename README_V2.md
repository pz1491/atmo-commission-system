# ระบบคำนวณคอมมิชชั่น ATMO'decor - Version 2.0

## 🎉 อัพเดทใหม่ในเวอร์ชัน 2.0

### ฟีเจอร์ใหม่
- ✅ **เริ่มต้นวันด้วยปฏิทิน** - เลือกวันที่จากปฏิทินแทนการใช้วันที่อัตโนมัติ
- ✅ **ระบุจำนวนคนตอบ** - แบ่งคอมมิชชั่นตามจำนวนคนที่ทำงาน
- ✅ **ระบุชื่อผู้ตอบ** - บันทึกชื่อคนที่ทำงานในแต่ละวัน
- ✅ **OT Penalty** - หักคอมมิชชั่น 30% (สูงสุด 300 บาท) ถ้ายอดช่วง 18:00-22:00 < 5,000 บาท
- ✅ **Incentive ต่อคน** - คำนวณคอมมิชชั่นต่อคนอัตโนมัติ
- ✅ **เก็บรูปภาพออเดอร์** - อัพโหลดและดูรูปภาพออเดอร์ย้อนหลังได้

### การคำนวณคอมมิชชั่นใหม่

#### คอมมิชชั่นหลัก (แก้ไข)
- **ยอดขั้นต่ำ: 25,000 บาท** (เดิม 20,000)
- 25,000-49,999 บาท → **1%** + โบนัส 3 ออเดอร์ (+100 บาท)
- 50,000-69,999 บาท → **2%** + โบนัส 6 ออเดอร์ (+400 บาท)
- 70,000-99,999 บาท → **2%** + โบนัส 8 ออเดอร์ (+800 บาท)
- 100,000-179,999 บาท → **3%**
- 180,000+ บาท → **4%** + โบนัส 12 ออเดอร์ (+1,500 บาท)

#### คอมมิชชั่นพิเศษ (5%)
- แจกันฟาแลน (ยกเว้น Ikebana Curve)
- Ikebana Curve ทุกรุ่น
- ชุดดอกไม้อย่างเดียว (≥8,000 บาท)

#### Add on (2vases) - แก้ไข
- แจกัน 2 ใบ+ (ราคา ≥4,500 บาท/ใบ)
  - **ยอด ≤ 9,500 บาท → +500 บาท**
  - **ยอด > 9,500 บาท → +300 บาท**
- ไม่รวม Mini vase

#### Add on (order)
- 3 ออเดอร์+ → +100 บาท
- 6 ออเดอร์+ → +400 บาท
- 8 ออเดอร์+ → +800 บาท
- 12 ออเดอร์+ → +1,500 บาท

#### OT Incentive (ใหม่!)
**ส่วนที่ 1: ช่วง 18:00-22:00**
- ต้องมียอดขาย ≥ 5,000 บาท
- ถ้าไม่ถึง → หักคอมมิชชั่น 30% (สูงสุด 300 บาท)

**ส่วนที่ 2: ช่วง 22:00-00:00**
- ทุก 6,000 บาท → +50 บาท/คน
- สูงสุด +300 บาท/คน

#### การแบ่งคอมมิชชั่น (ใหม่!)
- **คอมมิชชั่นรวม ÷ จำนวนคน = Incentive ต่อคน**

---

## 🚀 วิธีใช้งาน

### 1. เริ่มต้นวัน
ส่งคำสั่ง `/start` ในกลุ่ม LINE

ระบบจะถาม:
1. **วันที่** - เลือกจากปฏิทิน
2. **จำนวนคน** - เช่น 2, 3, 4 คน
3. **ชื่อผู้ตอบ** - เช่น Oil, Fang, Phung (คั่นด้วยคอมม่า)

### 2. ส่งออเดอร์
ส่งข้อความพร้อมรูปภาพ (ถ้ามี) โดยมีข้อมูล:
- ชื่อสินค้า (บรรทัดแรก)
- ยอดเงิน
- เวลา
- ข้อมูลลูกค้า

**ตัวอย่าง:**
```
1. แจกันดอกไม้ 1
25,000 บาท 13:40
คุณ ทดสอบ
...
```

### 3. ดูสรุปยอด
ส่งคำสั่ง `/summary` เพื่อดูสรุปยอดวันนี้

### 4. ดูรูปภาพ
ส่งคำสั่ง `/images` เพื่อดูรูปภาพออเดอร์ทั้งหมด

### 5. รีเซ็ตข้อมูล
ส่งคำสั่ง `/reset` เพื่อรีเซ็ตข้อมูล (ใช้ด้วยความระมัดระวัง)

---

## 📋 คำสั่งที่ใช้ได้

| คำสั่ง | คำอธิบาย |
|--------|----------|
| `/start` | เริ่มต้นวันใหม่ (ระบุวันที่, จำนวนคน, ชื่อ) |
| `/summary` | แสดงสรุปยอดวันนี้ |
| `/images` | ดูรูปภาพออเดอร์ทั้งหมด |
| `/reset` | รีเซ็ตข้อมูล |
| `/help` | แสดงคำสั่งทั้งหมด |

---

## 🔧 การติดตั้ง

### 1. ติดตั้ง Dependencies
```bash
pip3 install -r requirements.txt
```

### 2. ตั้งค่า Environment Variables
แก้ไขไฟล์ `.env`:
```
LINE_CHANNEL_ACCESS_TOKEN=your_channel_access_token
LINE_CHANNEL_SECRET=your_channel_secret
PORT=5000
```

### 3. รันระบบ
```bash
python3 app.py
```

### 4. ตั้งค่า Webhook URL
ไปที่ [LINE Developers Console](https://developers.line.biz/console/)
- เลือก Channel ของคุณ
- ไปที่ Messaging API
- ตั้งค่า Webhook URL: `https://your-domain.com/webhook`
- เปิดใช้งาน Webhook

### 5. เพิ่ม Bot เข้ากลุ่ม
- สแกน QR Code หรือเพิ่มเพื่อนด้วย LINE ID
- เชิญ Bot เข้ากลุ่ม LINE
- ส่งคำสั่ง `/start` เพื่อเริ่มใช้งาน

---

## 📊 ตัวอย่างการแสดงผล

```
✅ บันทึกออเดอร์สำเร็จ!

📦 ออเดอร์นี้:
• สินค้า: แจกันดอกไม้ 1
• ยอดขาย: 25,000 บาท
• เวลา: 13:40
• คอมมิชชั่น: 250 บาท (1%)

📊 สรุปวันนี้ (2026-01-11)
👥 คนตอบ: Oil, Fang (2 คน)

• ยอดขายรวม: 25,000 บาท
• จำนวนออเดอร์: 1 ออเดอร์
• เรทปัจจุบัน: 1%

💰 คอมมิชชั่น:
• คอมมิชชั่น 1-4%: 250 บาท
• คอมมิชชั่น 5%: 0 บาท
• Add on (2vases): 0 บาท
• Add on (order): 0 บาท

⏰ OT:
• ช่วง 18:00-22:00: 0 บาท ❌
• Penalty: 0 บาท

💵 รวมทั้งหมด: 250 บาท
💵 Incentive ต่อคน: 125.00 บาท
```

---

## 🧪 การทดสอบ

รันสคริปต์ทดสอบ:
```bash
python3 tests/test_commission_v2.py
```

---

## 📁 โครงสร้างโปรเจกต์

```
line-commission-system/
├── app.py                          # แอปพลิเคชันหลัก
├── requirements.txt                # Dependencies
├── .env                           # Environment variables
├── .env.example                   # ตัวอย่าง environment variables
├── README.md                      # คู่มือเวอร์ชัน 1.0
├── README_V2.md                   # คู่มือเวอร์ชัน 2.0 (ไฟล์นี้)
├── QUICKSTART.md                  # คู่มือเริ่มต้นใช้งาน
├── design.md                      # เอกสารออกแบบ v1
├── design-v2.md                   # เอกสารออกแบบ v2
├── src/
│   ├── __init__.py
│   ├── commission_calculator.py   # โมดูลคำนวณคอมมิชชั่น (v2)
│   ├── database.py                # โมดูลฐานข้อมูล (v2)
│   └── line_handler.py            # โมดูล LINE handler (v2)
├── tests/
│   ├── test_commission.py         # สคริปต์ทดสอบ v1
│   └── test_commission_v2.py      # สคริปต์ทดสอบ v2
└── data/                          # โฟลเดอร์เก็บข้อมูล
    ├── sales_data.json            # ข้อมูลยอดขาย
    └── images/                    # รูปภาพออเดอร์
```

---

## 🔄 การอัพเกรดจาก v1 เป็น v2

ระบบ v2 มีการเปลี่ยนแปลงโครงสร้างข้อมูลและการทำงาน:

### สิ่งที่เปลี่ยนแปลง
1. **ยอดขั้นต่ำ**: 20,000 → **25,000 บาท**
2. **เรท 70,000-99,999**: เพิ่มโบนัส 8 ออเดอร์ (+800 บาท)
3. **Add on (2vases)**: แบ่งเป็น 2 เกณฑ์ (≤9,500 → +500, >9,500 → +300)
4. **OT Penalty**: หัก 30% ถ้ายอด 18:00-22:00 < 5,000
5. **การแบ่งคอมมิชชั่น**: แบ่งตามจำนวนคนอัตโนมัติ

### การสำรองข้อมูล
ไฟล์เดิมถูกสำรองไว้:
- `app_v1_backup.py`
- `src/commission_calculator_backup.py`
- `src/database_backup.py`
- `src/line_handler_backup.py`

---

## ⚠️ ข้อควรระวัง

1. **ต้องเริ่มต้นวันก่อนใช้งาน** - ส่ง `/start` ทุกครั้งที่เริ่มวันใหม่
2. **ระบุจำนวนคนให้ถูกต้อง** - จะใช้ในการแบ่งคอมมิชชั่น
3. **ตรวจสอบเวลา** - ระบบจะใช้เวลาในข้อความหรือเวลาปัจจุบัน
4. **รูปภาพ** - ส่งรูปก่อนหรือหลังข้อความก็ได้ ระบบจะเชื่อมโยงอัตโนมัติ

---

## 📞 การติดต่อ

หากมีปัญหาหรือข้อสงสัย กรุณาติดต่อผู้พัฒนาระบบ

---

## 📄 License

Copyright © 2026 ATMO'decor. All rights reserved.
