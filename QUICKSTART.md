# 🚀 คู่มือเริ่มต้นใช้งานด่วน

## ขั้นตอนการติดตั้งและใช้งาน (5 นาที)

### 1️⃣ ติดตั้ง Dependencies

```bash
pip3 install -r requirements.txt
```

### 2️⃣ สร้าง LINE Official Account

1. ไปที่ https://developers.line.biz/console/
2. สร้าง Channel ประเภท "Messaging API"
3. คัดลอก **Channel Secret** และ **Channel Access Token**

### 3️⃣ ตั้งค่า Environment Variables

แก้ไขไฟล์ `.env`:

```bash
LINE_CHANNEL_ACCESS_TOKEN=ใส่_token_ที่คัดลอกมา
LINE_CHANNEL_SECRET=ใส่_secret_ที่คัดลอกมา
PORT=5000
TIMEZONE=Asia/Bangkok
```

### 4️⃣ รันระบบ

```bash
python3 app.py
```

### 5️⃣ ตั้งค่า Webhook

#### สำหรับทดสอบ (Local):

```bash
# ติดตั้ง ngrok จาก https://ngrok.com/
ngrok http 5000
```

คัดลอก URL ที่ได้ (เช่น `https://xxxx.ngrok-free.app`)

#### ตั้งค่าใน LINE Developers Console:

1. ไปที่ Channel Settings → Messaging API
2. Webhook URL: `https://your-url.com/webhook`
3. เปิด "Use webhook": ON
4. ปิด "Auto-reply messages": OFF

### 6️⃣ เพิ่ม Bot เข้ากลุ่ม

1. สแกน QR Code ของ LINE OA
2. เพิ่ม Bot เข้ากลุ่ม
3. ส่งข้อความออเดอร์ทดสอบ

---

## 📱 วิธีใช้งาน

### ส่งออเดอร์

ส่งข้อความออเดอร์ตามปกติ ระบบจะคำนวณอัตโนมัติ:

```
1. แจกันดอกไม้ 1
15,000 บาท
คุณ ทดสอบ
...
```

### คำสั่งพิเศษ

- `/summary` - ดูสรุปยอดวันนี้
- `/reset` - รีเซ็ตข้อมูล
- `/help` - ดูคำสั่งทั้งหมด

---

## 🔧 แก้ไขปัญหาเบื้องต้น

**Bot ไม่ตอบกลับ?**
- ตรวจสอบว่าปิด "Auto-reply messages" แล้ว
- ตรวจสอบว่า Token ถูกต้อง

**Webhook ไม่ทำงาน?**
- ตรวจสอบว่าเซิร์ฟเวอร์รันอยู่
- กด "Verify" ใน LINE Developers Console

---

## 📚 เอกสารเพิ่มเติม

อ่านเอกสารฉบับเต็ม: [README.md](README.md)

---

**เริ่มใช้งานได้เลย! 🎉**
